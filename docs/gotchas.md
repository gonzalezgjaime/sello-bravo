# Gotcha log

Append-only. One line per gotcha, recorded the moment it happens. Retrospectives dedupe and promote recurring entries to coach patterns.

| date | gotcha | root cause | fix / prevention |
|------|--------|------------|------------------|
| 2026-07-02 | Headless Chrome `--window-size=390` silently rendered a ~500px-wide layout cropped to 390px — mobile screenshots looked like the page overflowed horizontally when it didn't | Chrome (old and new headless, macOS) clamps window width to ~500px minimum; the screenshot is cropped to the requested width, so everything looks uniformly cut on the right | Never trust `--window-size` below 500 for viewport testing. Render the page inside a fixed-width `<iframe>` in a ≥500px window instead (see scratchpad `frame390.html` technique); verify suspected overflow with a media-query probe page before editing CSS |
| 2026-07-02 | Gotcha-coach hook flagged "edited index.html N times without converging → escalate to Opus" during a planned red→green TDD batch of ~25 small independent edits, firing on every edit | Coach counts raw Edit calls per file; it can't distinguish failed retry-iteration from a checklist batch where the gate is deliberately red until the batch lands | Coach should reset its per-file counter when a verification gate/script runs between edits, or suppress after the agent acknowledges the advisory with a stated batch plan; until then, treat the advisory as noise when edits are distinct checklist items and the gate hasn't been re-run yet |
| 2026-07-02 | Workflow tool `args` written as a JSON text block reached the script as one string — `args.dimensions` was undefined and pipeline() threw "expects an array" | The harness passes the `args` parameter verbatim; emitting it as a string parameter (rather than a structured JSON value) ships a stringified blob, exactly as the Workflow docs warn | Guard every workflow script with `const A = typeof args === 'string' ? JSON.parse(args) : args` and reference `A.*`; prefer embedding constants in the script body over `args` for one-off runs |
| 2026-07-02 | [wcag-contrast reviewer] Solid Cobre #C0492C can never host AA normal-size text: max possible ratio on it is 4.23:1 (pure black); Hueso on it is 4.16:1. DESIGN.md's own CTA-hover spec (Cobre fill + Tinta Profunda text) and the Cobre palette swatch labels are therefore unfixable by color choice alone | Cobre's relative luminance (0.161) sits in the dead zone where neither black nor white reaches 4.5:1; the spec was written from taste, not computed | For normal-size text on Cobre: lighten the fill (e.g. #D25538 → 4.62:1 with #10100E text) or make the text large (≥24px needs only 3:1, which #C0492C passes at 3.84:1). Alpha-composite + compute before speccing any text-on-Cobre surface |
| 2026-07-02 | [a11y-semantics reviewer] impeccable design-system-color hook fired on a scratchpad QA measurement harness (flagged #333/#fff iframe-scaffolding chrome as palette drift), forcing a justification detour mid-review | The hook matches every Write of HTML/CSS regardless of whether the file is shipped UI or throwaway test tooling in the session scratchpad | Treat design-hook findings on scratchpad/QA harness files as false positives by context and say so; longer-term, ignore-file the scratchpad path or scope the hook to repo surfaces |
| 2026-07-02 | [designmd-compliance reviewer] Tried to crop a QA screenshot with python3+PIL — macOS system python3 has no PIL/Pillow, cost a retry | Assumed Pillow is ubiquitous; the sandboxed system python is bare | Crop review screenshots by re-rendering the region with headless Chrome (adjust the frame390-style iframe wrapper height) or use `sips`; don't reach for PIL first |
| 2026-07-02 | [responsive-breakpoints reviewer] sips `-c H W --cropOffset y x` did not anchor to the given offset — it center-cropped, so 'hero' crops showed the product grid, costing a render cycle | macOS sips crop is center-based and this sips build silently ignores/reinterprets --cropOffset | Don't crop QA screenshots with sips offsets; re-render the exact region instead: iframe wrapper with fixed width, `margin-top:-<y>px`, body `overflow:hidden;height:<h>px`, then headless-Chrome screenshot at that window size |
| 2026-07-02 | [verifier-subagent] Headless-Chrome iframe measurement probe stayed "PENDING" (dump-dom showed no results) despite virtual-time budget | file:// parent cannot access file:// iframe contentDocument (unique-origin policy), so the measure script threw before writing output | Always add --allow-file-access-from-files when a file:// harness reads an iframe via contentDocument |
| 2026-07-02 | [adversarial-verifier] Same file:// iframe gotcha, second symptom variant: contentDocument stayed null so probe reported element "NOT FOUND" (not a hang) — also about:blank readyState==='complete' passes load checks before the real src loads | file:// unique-origin blocks iframe contentDocument without --allow-file-access-from-files; initial about:blank doc reads as complete | Add --allow-file-access-from-files AND poll for the target selector inside the iframe, never trust readyState/load alone; consult docs/gotchas.md before building a headless harness |
